# Day 05 - Lists (Python)

## ✅ Topics Covered
- Creating lists
- Adding elements using `append()`
- Indexing and slicing
- Finding length using `len()`
- Finding max/min (with and without built-in functions)
- Filtering invalid sensor readings
- Moving average (basic sensor smoothing)

---

## 📂 Programs Included

| File Name | Description |
|----------|-------------|
| `q1_list_details.py` | Input readings into list and print length, first & last element |
| `q2_max_min_without_builtin.py` | Find max and min using loop (without `max()` / `min()`) |
| `q3_filter_invalid_readings.py` | Ignore negative sensor readings and compute average |
| `q4_waypoints_list.py` | Store robot navigation waypoints as (x, y) tuples |
| `q5_moving_average.py` | Moving average smoothing for sensor readings |

---

## ▶️ How to Run
Example:
```bash
python q1_list_details.py
